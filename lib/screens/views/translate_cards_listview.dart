import 'package:flutter/material.dart';

class TranslateCardsListView extends StatelessWidget {
  const TranslateCardsListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
